<nav class="bg-purple-900 text-white flex justify-between">
  <ul class="px-10 py-4 flex space-x-3">
    <%= content_tag(:li, class: "cursor-pointer") do link_to("HOME", root_path) end if !admin_signed_in? %>
    <%= content_tag(:li, class: "cursor-pointer") do link_to("ROOM", root_path) end if admin_signed_in? %>
    <%= content_tag(:li, class: "cursor-pointer") do link_to("Calender", calenders_path) end if admin_signed_in? %>
    <%= content_tag(:li, class: "cursor-pointer") do link_to("Register", new_user_registration_path) end if !user_signed_in? && !admin_signed_in? %>
    <%= content_tag(:li, class: "cursor-pointer") do link_to("Login", new_user_session_path) end if !user_signed_in? && !admin_signed_in? %>
    <%= content_tag(:li, class: "cursor-pointer") do link_to("Admin Register", new_admin_registration_path) end if !user_signed_in? && !admin_signed_in? %>
    <%= content_tag(:li, class: "cursor-pointer") do link_to("Admin Login", new_admin_session_path) end if !user_signed_in? && !admin_signed_in? %>
    <%= content_tag(:li, class: "cursor-pointer") do link_to("Logout", destroy_user_session_path, data: {turbo_method: :delete}) end if user_signed_in? %>
    <%= content_tag(:li, class: "cursor-pointer") do link_to("Admin Logout", destroy_user_session_path, data: {turbo_method: :delete}) end if admin_signed_in? %>
  </ul>
</nav>
