<nav class"bg-purple-900 text-white flex justify-between">
  <ul class="px-10 py-4 flex space-x-3">
    <%= content_tag(:li, class: "cursor-pointer") do link_to("HOME", root_path) end %>
    <%= content_tag(:li, class: "cursor-pointer") do link_to("Calender", calenders_path) end %>
    <%= content_tag(:li, class: "cursor-pointer") do link_to("Register", new_user_registration_path) end %>
    <%= content_tag(:li, class: "cursor-pointer") do link_to("Login", new_user_session_path) end %>
    <%= content_tag(:li, class: "cursor-pointer") do link_to("Admin Login", new_admin_session_path) end %>
  </ul>
</nav>
